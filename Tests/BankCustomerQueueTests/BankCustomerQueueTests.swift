import XCTest
@testable import BankCustomerQueue

final class BankCustomerQueueTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(BankCustomerQueue().text, "Hello, World!")
    }
}
